function slasher(arr, howMany) {
  // it doesn't always pay to be first
  if (howMany > 0 ) {
  arr = arr.splice(howMany, arr.length);
  return arr;
  }
  else {
    return arr;
  }
}

slasher(["Banana", "Orange", "Apple", "Mango"], 2);
