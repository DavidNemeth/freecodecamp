function palindrome(str) {
  // Good luck!    
  str = str.replace(/[^\w]/g,'');
  str = str.toLowerCase();  
  var arr = str.split("");
  arr.reverse();
  arr = arr.join("");
  if (arr ==str) {
    return true;
  }
  else {
    return false;
  }
}



palindrome("A man, a plan, a canal. Panama");
